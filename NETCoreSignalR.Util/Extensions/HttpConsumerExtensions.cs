using NETCoreSignalR.Domain.Interfaces;
using System;
using System.Collections.Generic;
using System.Net;
using System.Text;

namespace NETCoreSignalR.Util.Extensions
{
    public static class HttpConsumerExtensions
    {
        //public static void AddCookies(this IHttpConsumer a, List<Cookie> cookies)
        //{
        //    _request.CookieContainer = new CookieContainer();

        //    cookies.ForEach(cookie => _request.CookieContainer.Add(cookie));
        //}

        //public static void AddHeaders(List<KeyValuePair<string, string>> headers)
        //{
        //    headers.ForEach(header => _request.AddDefaultHeader(header.Key, header.Value));
        //}
        //public static void AddHeader(KeyValuePair<string, string> header)
        //{
        //    _request.AddDefaultHeader(header.Key, header.Value);
        //}
    }
}
